---
/**
 * Quiz Detail Page
 *
 * Renders an individual quiz with questions and interactive quiz logic.
 */
import Layout from "@layouts/Layout.astro";
import Headline from "@shared-ui/components/typography/Headline.astro";
import Icon from "@shared-ui/components/visual/Icon.astro";
import PageShell from "@shared-ui/components/layout/PageShell.astro";
import { buildPageSeo, type StructuredData } from "@shared-utils/utils/seo/buildPageSeo";
import { getCollectionEntries } from "@shared-utils/utils/content/getCollectionEntries";
import {
  buildBreadcrumbListSchema,
  buildQuizSchema,
  buildSpeakableSpecificationSchema,
} from "@shared-utils/utils/seo/seoSchema";
import { resolveBaseUrl, resolvePageUrl } from "@shared-utils/utils/siteUrls";
import type { QuizData, QuizQuestion } from "../content.config";
import { getQuizImageUrl, quizHeroImageUrl } from "../utils/quizImages";

export const prerender = true;

interface Props {
  quiz: {
    id: string;
    data: QuizData;
  };
}

export async function getStaticPaths() {
  const quizzes = await getCollectionEntries("quizzes");
  return quizzes.map((quiz: any) => ({
    params: { slug: quiz.id },
    props: { quiz: { id: quiz.id, data: quiz.data } },
  }));
}

const { quiz } = Astro.props as Props;

const baseUrl = resolveBaseUrl(Astro.site);
const currentUrl = resolvePageUrl(Astro.site, `/${quiz.id}`);

const seoBreadcrumbs = [
  { name: "Home", url: resolvePageUrl(Astro.site, "/") },
  { name: quiz.data.title, url: currentUrl },
];

const breadcrumbSchema = buildBreadcrumbListSchema(seoBreadcrumbs);

const quizSeoTitle = quiz.data.seoTitle || `${quiz.data.title} Music Quiz`;
const quizSeoDescription = quiz.data.seoDescription || quiz.data.description;
const quizImage = getQuizImageUrl(quiz.id, baseUrl) ?? quizHeroImageUrl(baseUrl);
const quizFeaturedTopics = quiz.data.featuredTopics.length
  ? quiz.data.featuredTopics
  : [quiz.data.category.replace(/-/g, " "), ...quiz.data.relatedArticles];

const quizSchema = buildQuizSchema({
  title: quiz.data.title,
  description: quizSeoDescription,
  url: currentUrl,
  numberOfQuestions: quiz.data.questionsPerSession,
  topics: quizFeaturedTopics,
  keywords: quizFeaturedTopics,
});

const speakableSchema = buildSpeakableSpecificationSchema({
  cssSelectors: [
    "h1",
    ".quiz-detail__panel-header + p",
    ".quiz-detail__panel-header + div p:first-of-type",
  ],
});

const structuredDataList = [
  breadcrumbSchema,
  quizSchema,
  ...(speakableSchema ? [speakableSchema] : []),
] as StructuredData[];

const pageSeo = buildPageSeo({
  title: quizSeoTitle,
  description: quizSeoDescription,
  url: currentUrl,
  image: quizImage,
  imageAlt: `Cover image for the quiz: ${quiz.data.title}`,
  contentKind: "generic",
  structuredData: structuredDataList,
  authorName: "MelodyMind Quiz",
  maxImagePreview: "large",
  autoSocialImage: true,
  generateSocialImage: ({ title: genTitle }) => {
    void genTitle;
    return undefined;
  },
  brandSuffix: " - MelodyMind Quiz",
});

const questionsData = quiz.data.questions.map((q: QuizQuestion) => ({
  question: q.question,
  type: q.type,
  difficulty: q.difficulty,
  options: q.options,
  correct: q.correct,
  explanation: q.explanation,
  source: q.source,
  sourceLine: q.sourceLine,
}));

const passingScore = quiz.data.passingScore;
const questionsPerSession = quiz.data.questionsPerSession;
const totalPoolSize = quiz.data.questions.length;
---

<Layout {pageSeo} showHeaderIcons={false} showFooter={false}>
  <PageShell
    className="page-shell--aurora page-shell--grid"
    containerClass="page-shell__container--narrow"
  >
    <div class="quiz-detail">
      <div class="quiz-detail__content">
        <main class="quiz-detail__main">
          <h1 class="quiz-detail__sr-title">{quiz.data.title}</h1>

          <section class="quiz-detail__progress-shell" aria-label="Quiz progress">
            <div id="quiz-progress-container"></div>
          </section>

          <section class="quiz-detail__panel" aria-labelledby="quiz-play-heading">
            <div class="quiz-detail__panel-header">
              <Headline level="h2" textSize="lg" className="quiz-detail__panel-title">
                <span id="quiz-play-heading">Answer the question</span>
              </Headline>
            </div>

            <div id="quiz-question-container"></div>
            <div id="quiz-result-container"></div>
          </section>

          <div class="quiz-detail__nav" id="quiz-nav">
            <button
              type="button"
              id="quiz-nav-prev"
              class="quiz-detail__nav-btn quiz-detail__nav-btn--prev"
              hidden
            >
              <Icon name="arrow-left" class="quiz-detail__nav-icon" aria-hidden="true" />
              Previous
            </button>

            <button
              type="button"
              id="quiz-check-answer"
              class="quiz-detail__nav-btn quiz-detail__nav-btn--check"
            >
              Check Answer
            </button>

            <button
              type="button"
              id="quiz-nav-next"
              class="quiz-detail__nav-btn quiz-detail__nav-btn--next"
            >
              Next
              <Icon name="arrow-right" class="quiz-detail__nav-icon" aria-hidden="true" />
            </button>

            <button
              type="button"
              id="quiz-nav-submit"
              class="quiz-detail__nav-btn quiz-detail__nav-btn--submit"
              hidden
            >
              <Icon
                name="tabler:circle-check"
                class="quiz-detail__nav-icon"
                aria-hidden="true"
              />
              Show Results
            </button>
          </div>
        </main>
      </div>
    </div>
  </PageShell>

  <script
    is:inline
    type="application/json"
    id="quiz-questions-data"
    set:html={JSON.stringify(questionsData)}
  />
  <span id="quiz-passing-score" data-value={passingScore} hidden></span>
  <span id="quiz-questions-per-session" data-value={questionsPerSession} hidden></span>
  <span id="quiz-total-pool-size" data-value={totalPoolSize} hidden></span>
</Layout>

<script>
  import { initQuiz, type QuizQuestion } from "../scripts/quiz/quizClient";

  document.addEventListener("DOMContentLoaded", () => {
    try {
      const questionsEl = document.getElementById("quiz-questions-data");
      const passingScoreEl = document.getElementById("quiz-passing-score");
      const questionsPerSessionEl = document.getElementById("quiz-questions-per-session");

      if (!questionsEl || !passingScoreEl || !questionsPerSessionEl) {
        console.warn("[quiz] Missing quiz data elements");
        return;
      }

      const questions: QuizQuestion[] = JSON.parse(questionsEl.textContent || "[]");
      const passingScore: number = parseInt(passingScoreEl.dataset.value || "70", 10);
      const questionsPerSession: number = parseInt(
        questionsPerSessionEl.dataset.value || "20",
        10
      );

      initQuiz(questions, passingScore, questionsPerSession);
    } catch (e) {
      console.error("[quiz] Failed to initialize quiz:", e);
    }
  });
</script>

<style>
  .quiz-detail {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-base);
  }

  .quiz-detail__header {
    position: relative;
  }

  .quiz-detail__hero {
    display: grid;
    gap: var(--stack-gap-sm);
    padding: var(--spacing-sm) 0 calc(var(--spacing-lg) - 0.75rem);
    border-block-end: 1px solid var(--surface-border-soft);
  }

  @media (width >= 960px) {
    .quiz-detail__hero {
      grid-template-columns: minmax(0, 1fr) minmax(12rem, 14rem);
      gap: calc(var(--spacing-sm) + 0.35rem);
      align-items: center;
      padding: 0 0 calc(var(--spacing-lg) - 0.75rem);
    }
  }

  .quiz-detail__hero-main {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-sm) + 0.25rem);
  }

  .quiz-detail__breadcrumb a {
    font-size: var(--font-size-base);
    color: var(--text-secondary);
    text-decoration: none;
  }

  .quiz-detail__breadcrumb a:hover,
  .quiz-detail__breadcrumb a:focus-visible {
    color: var(--accent-primary);
  }

  .quiz-detail__title {
    margin: 0;
    font-size: var(--font-size-xl);
    line-height: 1.04;
    color: var(--text-primary);
    letter-spacing: -0.045em;
  }

  .quiz-detail__meta {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(10rem, max-content));
    gap: var(--spacing-sm);
  }

  .quiz-detail__badge {
    display: inline-flex;
    align-items: center;
    min-block-size: var(--control-min-block-size-compact);
    padding: var(--control-padding-block-compact)
      calc(var(--control-padding-inline-compact) - 0.2rem);
    font-size: var(--font-size-sm);
    font-weight: 700;
    color: var(--text-secondary);
    background: color-mix(in srgb, var(--surface-1) 74%, transparent);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-xs);
  }

  .quiz-detail__hero-media {
    position: relative;
    justify-self: end;
    max-inline-size: 11rem;
    min-block-size: 6.5rem;
    overflow: hidden;
    background: var(--surface-panel-bg);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
    opacity: 0.9;
  }

  @media (width <= 959px) {
    .quiz-detail__hero-media {
      display: none;
    }
  }

  .quiz-detail__hero-image {
    display: block;
    inline-size: 100%;
    block-size: 100%;
    object-fit: cover;
  }

  .quiz-detail__content {
    display: flex;
    flex-direction: column;
    gap: var(--stack-gap-md);
  }

  @media (width <= 639px) {
    .quiz-detail__content {
      gap: calc(var(--spacing-base) + 0.1rem);
    }
  }

  .quiz-detail__progress-shell,
  .quiz-detail__panel,
  .quiz-detail__footer {
    background: var(--surface-panel-bg);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
  }

  .quiz-detail__progress-shell,
  .quiz-detail__panel {
    padding: var(--panel-padding-sm);
  }

  .quiz-detail__panel {
    margin-block-start: 1rem;
  }

  @media (width >= 640px) {
    .quiz-detail__progress-shell,
    .quiz-detail__panel {
      padding: 0.9rem;
    }

    .quiz-detail__panel {
      margin-block-start: 1.25rem;
    }
  }

  .quiz-detail__panel-header {
    margin-block-end: 0.35rem;
  }

  .quiz-detail__panel-title,
  .quiz-detail__footer-title {
    margin: 0;
    color: var(--text-primary);
    letter-spacing: -0.03em;
  }

  .quiz-detail__panel-title {
    font-size: var(--font-size-base);
    color: var(--text-secondary);
  }

  .quiz-detail__nav {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-sm) + 0.05rem);
    margin-block-start: calc(var(--spacing-sm) + 0.2rem);
  }

  @media (width >= 640px) {
    .quiz-detail__nav {
      flex-flow: row wrap;
      justify-content: center;
    }
  }

  @media screen and (prefers-reduced-motion: reduce) {
    .quiz-detail__nav-btn {
      display: inline-flex;
      gap: calc(var(--spacing-sm) + 0.05rem);
      align-items: center;
      justify-content: center;
      inline-size: 100%;
      min-block-size: var(--control-min-block-size);
      padding: calc(var(--spacing-sm) + 0.3rem) var(--spacing-base);
      font-weight: 700;
      color: var(--text-primary);
      cursor: pointer;
      background: color-mix(in srgb, var(--surface-1) 76%, transparent);
      border: 1px solid var(--surface-border-soft);
      border-radius: var(--radius-lg);
      transition: none;
    }
  }

  .quiz-detail__nav-btn {
    display: inline-flex;
    gap: calc(var(--spacing-sm) + 0.05rem);
    align-items: center;
    justify-content: center;
    inline-size: 100%;
    min-block-size: var(--control-min-block-size);
    padding: calc(var(--spacing-sm) + 0.3rem) var(--spacing-base);
    font-weight: 700;
    color: var(--text-primary);
    cursor: pointer;
    background: color-mix(in srgb, var(--surface-1) 76%, transparent);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xs);
    transition:
      transform var(--transition-fast),
      border-color var(--transition-fast),
      background-color var(--transition-fast),
      box-shadow var(--transition-fast);
  }

  @media (width >= 640px) {
    .quiz-detail__nav-btn {
      inline-size: auto;
      min-inline-size: 10rem;
    }
  }

  .quiz-detail__nav-btn:disabled {
    cursor: not-allowed;
    opacity: 0.5;
  }

  .quiz-detail__nav-btn:focus-visible,
  .quiz-detail__nav-btn:hover:not(:disabled) {
    border-color: var(--surface-border-accent);
    box-shadow: var(--interactive-shadow-focus);
    transform: translateY(-1px);
  }

  .quiz-detail__sr-title {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    white-space: nowrap;
    border: 0;
    clip-path: inset(50%);
  }

  .quiz-detail__nav-btn--check,
  .quiz-detail__nav-btn--submit {
    color: var(--accent-contrast);
    background: var(--accent-primary);
  }

  .quiz-detail__nav-icon {
    flex: 0 0 auto;
    inline-size: var(--spacing-base);
    block-size: var(--spacing-base);
  }

  .quiz-detail__footer {
    padding: calc(var(--spacing-sm) + 0.15rem) calc(var(--spacing-base) - 0.15rem);
  }

  .quiz-detail__footer-head {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .quiz-detail__source {
    margin: 0;
    font-size: var(--font-size-base);
    line-height: 1.5;
    color: var(--text-secondary);
  }

  :global(#quiz-progress-container .quiz-progress) {
    display: grid;
    gap: calc(var(--spacing-sm) + 0.15rem);
  }

  @media (width >= 768px) {
    :global(#quiz-progress-container .quiz-progress) {
      grid-template-columns: minmax(0, 1.2fr) minmax(14rem, 0.8fr);
      column-gap: 1rem;
      align-items: start;
    }
  }

  :global(#quiz-progress-container .quiz-progress__eyebrow) {
    margin: 0;
    font-size: var(--font-size-base);
    font-weight: 700;
    color: var(--accent-primary);
    text-transform: uppercase;
    letter-spacing: 0.14em;
  }

  @media (width >= 768px) {
    :global(#quiz-progress-container .quiz-progress__eyebrow) {
      grid-row: 1;
      grid-column: 1 / -1;
    }
  }

  :global(#quiz-progress-container .quiz-progress__headline),
  :global(#quiz-progress-container .quiz-progress__stats) {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-xs) + 0.1rem);
  }

  @media (width >= 768px) {
    :global(#quiz-progress-container .quiz-progress__headline) {
      grid-row: 2;
      grid-column: 1;
    }

    :global(#quiz-progress-container .quiz-progress__bar) {
      grid-row: 3;
      grid-column: 1 / -1;
    }

    :global(#quiz-progress-container .quiz-progress__stats) {
      grid-row: 4;
      grid-column: 1 / -1;
    }
  }

  :global(#quiz-progress-container .quiz-progress__index) {
    font-size: var(--font-size-base);
    font-weight: 700;
    color: var(--text-primary);
  }

  :global(#quiz-progress-container .quiz-progress__target),
  :global(#quiz-progress-container .quiz-progress__stat-label) {
    font-size: var(--font-size-base);
    color: var(--text-secondary);
  }

  :global(#quiz-progress-container .quiz-progress__target) {
    display: none;
  }

  @media (width >= 768px) {
    :global(#quiz-progress-container .quiz-progress__target) {
      display: block;
    }
  }

  :global(#quiz-progress-container .quiz-progress__bar) {
    block-size: 0.8rem;
    overflow: hidden;
    background: color-mix(in srgb, var(--surface-border-soft) 88%, transparent);
    border-radius: var(--radius-md);
  }

  @media screen and (prefers-reduced-motion: reduce) {
    :global(#quiz-progress-container .quiz-progress__fill) {
      block-size: 100%;
      background: linear-gradient(90deg, var(--accent-primary), var(--accent-strong));
      border-radius: inherit;
      transition: none;
    }
  }

  :global(#quiz-progress-container .quiz-progress__fill) {
    block-size: 100%;
    background: linear-gradient(90deg, var(--accent-primary), var(--accent-strong));
    border-radius: inherit;
    transition: inline-size var(--transition-fast);
  }

  :global(#quiz-progress-container .quiz-progress__stats) {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: calc(var(--spacing-xs) + 0.2rem);
  }

  @media (width <= 639px) {
    :global(#quiz-progress-container .quiz-progress__stats) {
      grid-template-columns: 1fr;
      gap: calc(var(--spacing-xs) + 0.15rem);
    }
  }

  :global(#quiz-progress-container .quiz-progress__stat) {
    display: flex;
    flex-direction: row;
    gap: var(--stack-gap-sm);
    align-items: baseline;
    justify-content: space-between;
    padding: var(--spacing-sm) calc(var(--spacing-sm) + 0.1rem);
    background: color-mix(in srgb, var(--surface-1) 74%, transparent);
    border: 1px solid var(--surface-border-subtle);
    border-radius: var(--radius-md);
  }

  @media (width <= 639px) {
    :global(#quiz-progress-container .quiz-progress__stat) {
      align-items: center;
      padding: calc(var(--spacing-sm) + 0.05rem) var(--spacing-md);
      border-radius: var(--radius-md);
    }
  }

  :global(#quiz-progress-container .quiz-progress__stat-label) {
    white-space: nowrap;
  }

  @media (width <= 639px) {
    :global(#quiz-progress-container .quiz-progress__stat-label) {
      line-height: 1.25;
      white-space: normal;
    }
  }

  :global(#quiz-progress-container .quiz-progress__stat-value) {
    font-size: var(--font-size-base);
    font-weight: 700;
    color: var(--text-primary);
    white-space: nowrap;
  }

  :global(#quiz-question-container .quiz-question) {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-sm) + 0.35rem);
  }

  :global(#quiz-question-container .quiz-question__topline) {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm);
    align-items: center;
  }

  :global(#quiz-question-container .quiz-question__number),
  :global(#quiz-question-container .quiz-question__type),
  :global(#quiz-question-container .quiz-question__badge) {
    display: inline-flex;
    gap: calc(var(--spacing-xs) + 0.08rem);
    align-items: center;
    min-block-size: var(--control-min-block-size-compact);
    padding: calc(var(--spacing-sm) - 0.15rem) var(--control-padding-inline-compact);
    font-size: var(--font-size-sm);
    font-weight: 700;
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-md);
  }

  :global(#quiz-question-container .quiz-question__number) {
    color: var(--text-primary);
    background: color-mix(in srgb, var(--accent-primary) 12%, var(--surface-1));
    border-color: var(--surface-border-accent);
  }

  :global(#quiz-question-container .quiz-question__type) {
    color: var(--text-secondary);
    background: color-mix(in srgb, var(--surface-1) 74%, transparent);
  }

  :global(#quiz-question-container .quiz-question__badge--correct) {
    color: var(--text-primary);
    background: color-mix(in srgb, var(--color-success) 18%, var(--surface-1));
    border-color: color-mix(
      in srgb,
      var(--color-success) 56%,
      var(--surface-border-soft)
    );
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-success) 18%, transparent);
  }

  :global(#quiz-question-container .quiz-question__badge--incorrect) {
    color: var(--text-primary);
    background: color-mix(in srgb, var(--color-error) 16%, var(--surface-1));
    border-color: color-mix(in srgb, var(--color-error) 56%, var(--surface-border-soft));
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-error) 16%, transparent);
  }

  :global(#quiz-question-container .quiz-question__badge svg) {
    inline-size: var(--spacing-base);
    block-size: var(--spacing-base);
  }

  :global(#quiz-question-container .quiz-question__card) {
    display: flex;
    flex-direction: column;
    gap: var(--stack-gap-sm);
    padding: 0;
    background: transparent;
    border: 0;
    border-radius: 0;
  }

  :global(#quiz-question-container .quiz-question__prompt),
  :global(#quiz-question-container .quiz-question__hint) {
    margin: 0;
    color: var(--text-secondary);
  }

  :global(#quiz-question-container .quiz-question__prompt) {
    font-size: var(--font-size-base);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.14em;
  }

  :global(#quiz-question-container .quiz-question__text) {
    margin: 0;
    font-size: var(--font-size-md);
    line-height: 1.4;
    color: var(--text-primary);
    letter-spacing: -0.03em;
  }

  :global(#quiz-question-container .quiz-question__options) {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-sm) + 0.15rem);
  }

  @media screen and (prefers-reduced-motion: reduce) {
    :global(#quiz-question-container .quiz-question__option) {
      display: grid;
      grid-template-columns: auto minmax(0, 1fr) auto;
      gap: calc(var(--spacing-sm) + 0.2rem);
      align-items: center;
      inline-size: 100%;
      padding: calc(var(--spacing-sm) + 0.35rem) calc(var(--spacing-base) - 0.1rem);
      color: var(--text-primary);
      text-align: start;
      cursor: pointer;
      background: color-mix(in srgb, var(--surface-1) 76%, transparent);
      border: 1px solid var(--surface-border-soft);
      border-radius: var(--radius-lg);
      transition: none;
    }
  }

  :global(#quiz-question-container .quiz-question__option) {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    gap: calc(var(--spacing-sm) + 0.2rem);
    align-items: center;
    inline-size: 100%;
    padding: calc(var(--spacing-sm) + 0.35rem) calc(var(--spacing-base) - 0.1rem);
    color: var(--text-primary);
    text-align: start;
    cursor: pointer;
    background: color-mix(in srgb, var(--surface-1) 76%, transparent);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xs);
    transition:
      transform var(--transition-fast),
      border-color var(--transition-fast),
      background-color var(--transition-fast),
      box-shadow var(--transition-fast);
  }

  @media (width <= 639px) {
    :global(#quiz-question-container .quiz-question__option) {
      min-block-size: 3.5rem;
      padding: 1rem;
    }
  }

  :global(#quiz-question-container .quiz-question__option:hover:not(:disabled)),
  :global(#quiz-question-container .quiz-question__option:focus-visible) {
    border-color: var(--surface-border-accent);
    transform: translateY(-1px);
  }

  :global(#quiz-question-container .quiz-question__option:focus-visible) {
    outline: var(--focus-ring-width) solid var(--focus-ring-color);
    outline-offset: var(--focus-ring-offset);
  }

  :global(#quiz-question-container .quiz-question__option:disabled) {
    cursor: default;
  }

  :global(#quiz-question-container .quiz-question__option--selected) {
    background: linear-gradient(
      90deg,
      color-mix(in srgb, var(--accent-primary) 14%, transparent),
      color-mix(in srgb, var(--accent-primary) 8%, var(--surface-1))
    );
    border-color: color-mix(
      in srgb,
      var(--accent-primary) 52%,
      var(--surface-border-soft)
    );
    box-shadow:
      inset 0.28rem 0 0 color-mix(in srgb, var(--accent-primary) 88%, transparent),
      inset 0 0 0 1px color-mix(in srgb, var(--accent-primary) 14%, transparent);
  }

  :global(#quiz-question-container .quiz-question__option--correct) {
    background: linear-gradient(
      90deg,
      color-mix(in srgb, var(--color-success) 18%, transparent),
      color-mix(in srgb, var(--color-success) 8%, var(--surface-1))
    );
    border-color: color-mix(
      in srgb,
      var(--color-success) 58%,
      var(--surface-border-soft)
    );
    box-shadow:
      inset 0.28rem 0 0 color-mix(in srgb, var(--color-success) 88%, transparent),
      inset 0 0 0 1px color-mix(in srgb, var(--color-success) 16%, transparent);
  }

  :global(#quiz-question-container .quiz-question__option--incorrect) {
    background: linear-gradient(
      90deg,
      color-mix(in srgb, var(--color-error) 16%, transparent),
      color-mix(in srgb, var(--color-error) 7%, var(--surface-1))
    );
    border-color: color-mix(in srgb, var(--color-error) 58%, var(--surface-border-soft));
    box-shadow:
      inset 0.28rem 0 0 color-mix(in srgb, var(--color-error) 88%, transparent),
      inset 0 0 0 1px color-mix(in srgb, var(--color-error) 14%, transparent);
  }

  :global(#quiz-question-container .quiz-question__option-marker),
  :global(#quiz-question-container .quiz-question__option-status) {
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  :global(#quiz-question-container .quiz-question__option-marker svg),
  :global(#quiz-question-container .quiz-question__option-status svg),
  :global(#quiz-question-container .quiz-question__explanation-header svg),
  :global(#quiz-result-container .quiz-result__btn svg) {
    inline-size: calc(var(--spacing-base) + var(--spacing-2xs));
    block-size: calc(var(--spacing-base) + var(--spacing-2xs));
  }

  :global(#quiz-question-container .quiz-question__option-text) {
    font-size: var(--font-size-base);
    line-height: 1.4;
  }

  :global(#quiz-question-container .quiz-question__option-status--correct) {
    color: var(--color-success);
  }

  :global(#quiz-question-container .quiz-question__option-status--incorrect) {
    color: var(--color-error);
  }

  :global(#quiz-question-container .quiz-question__explanation) {
    padding: calc(var(--spacing-sm) + 0.35rem) calc(var(--spacing-base) - 0.05rem);
    background: color-mix(in srgb, var(--surface-1) 78%, transparent);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
  }

  :global(#quiz-question-container .quiz-question__explanation-header) {
    display: inline-flex;
    gap: calc(var(--spacing-sm) + 0.05rem);
    align-items: center;
    margin-block-end: 0.65rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  :global(#quiz-question-container .quiz-question__explanation-text) {
    margin: 0;
    line-height: 1.6;
    color: var(--text-secondary);
  }

  :global(#quiz-result-container .quiz-result) {
    display: flex;
    flex-direction: column;
    gap: var(--stack-gap-md);
    padding: var(--panel-padding-md);
    background: var(--surface-panel-bg);
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
  }

  :global(#quiz-result-container .quiz-result--passed) {
    border-color: color-mix(
      in srgb,
      var(--color-success) 34%,
      var(--surface-border-soft)
    );
  }

  :global(#quiz-result-container .quiz-result--failed) {
    border-color: color-mix(in srgb, var(--color-error) 34%, var(--surface-border-soft));
  }

  :global(#quiz-result-container .quiz-result__header),
  :global(#quiz-result-container .quiz-result__score) {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-sm) + 0.25rem);
    align-items: center;
    text-align: center;
  }

  :global(#quiz-result-container .quiz-result__icon) {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    inline-size: calc(var(--icon-size-xl) * 2);
    block-size: calc(var(--icon-size-xl) * 2);
    color: var(--text-primary);
    background: var(--surface-chip-bg);
    border-radius: 50%;
  }

  :global(#quiz-result-container .quiz-result__icon svg) {
    inline-size: var(--spacing-xl);
    block-size: var(--spacing-xl);
  }

  :global(#quiz-result-container .quiz-result__title) {
    margin: 0;
    font-size: var(--font-size-xl);
    color: var(--text-primary);
    letter-spacing: -0.03em;
  }

  :global(#quiz-result-container .quiz-result__subtitle),
  :global(#quiz-result-container .quiz-result__score-text),
  :global(#quiz-result-container .quiz-result__passing) {
    margin: 0;
    line-height: 1.6;
    color: var(--text-secondary);
  }

  :global(#quiz-result-container .quiz-result__summary) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(9rem, 1fr));
    gap: calc(var(--spacing-sm) + 0.25rem);
  }

  :global(#quiz-result-container .quiz-result__summary-item) {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing-xs) + 0.15rem);
    padding: calc(var(--spacing-sm) + 0.4rem);
    text-align: center;
    background: var(--surface-chip-bg);
    border: 1px solid var(--surface-border-subtle);
    border-radius: var(--radius-lg);
  }

  :global(#quiz-result-container .quiz-result__summary-label) {
    font-size: var(--font-size-base);
    color: var(--text-secondary);
  }

  :global(#quiz-result-container .quiz-result__summary-value),
  :global(#quiz-result-container .quiz-result__score-value) {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--text-primary);
  }

  :global(#quiz-result-container .quiz-result__score-circle) {
    position: relative;
    display: inline-grid;
    place-items: center;
  }

  :global(#quiz-result-container .quiz-result__score-value) {
    position: absolute;
  }

  :global(#quiz-result-container .quiz-result__actions) {
    display: flex;
    flex-wrap: wrap;
    gap: calc(var(--spacing-sm) + 0.25rem);
    justify-content: center;
  }

  @media screen and (prefers-reduced-motion: reduce) {
    :global(#quiz-result-container .quiz-result__btn) {
      display: inline-flex;
      gap: calc(var(--spacing-sm) + 0.05rem);
      align-items: center;
      justify-content: center;
      min-block-size: var(--control-min-block-size);
      padding: calc(var(--spacing-sm) + 0.3rem) calc(var(--spacing-base) + 0.15rem);
      font-weight: 700;
      text-decoration: none;
      border: 1px solid var(--surface-border-soft);
      border-radius: var(--radius-lg);
      transition: none;
    }
  }

  :global(#quiz-result-container .quiz-result__btn) {
    display: inline-flex;
    gap: calc(var(--spacing-sm) + 0.05rem);
    align-items: center;
    justify-content: center;
    min-block-size: var(--control-min-block-size);
    padding: calc(var(--spacing-sm) + 0.3rem) calc(var(--spacing-base) + 0.15rem);
    font-weight: 700;
    text-decoration: none;
    border: 1px solid var(--surface-border-soft);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xs);
    transition:
      transform var(--transition-fast),
      border-color var(--transition-fast),
      background-color var(--transition-fast),
      box-shadow var(--transition-fast);
  }

  :global(#quiz-result-container .quiz-result__btn:hover),
  :global(#quiz-result-container .quiz-result__btn:focus-visible) {
    border-color: var(--surface-border-accent);
    transform: translateY(-1px);
  }

  :global(#quiz-result-container .quiz-result__btn:focus-visible) {
    outline: var(--focus-ring-width) solid var(--focus-ring-color);
    outline-offset: var(--focus-ring-offset);
  }

  :global(#quiz-result-container .quiz-result__btn--primary) {
    color: var(--accent-contrast);
    background: var(--accent-primary);
  }

  :global(#quiz-result-container .quiz-result__btn--secondary) {
    color: var(--text-primary);
    background: color-mix(in srgb, var(--surface-1) 76%, transparent);
  }
</style>
