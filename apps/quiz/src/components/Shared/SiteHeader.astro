---
import SharedSiteHeader from "@shared-ui/components/navigation/SiteHeader.astro";
import type { NavItem } from "@shared-ui/components/navigation/types";

interface Props {
  showIcons?: boolean;
}

const { showIcons = true } = Astro.props as Props;

const navItems: NavItem[] = [{ href: "/", label: "All Quizzes", icon: "grid" }];
---

<SharedSiteHeader
  {showIcons}
  {navItems}
  brandHref="/"
  brandText="Melody Mind"
  brandMark="MM"
  brandAriaLabel="Go to the Melody Mind homepage"
  searchAction="/search"
  searchInputName="q"
  searchInputLabel="Search Melody Mind quizzes"
  searchInputPlaceholder="Search quiz titles or descriptions..."
  searchSubmitLabel="Search"
  menuOpenLabel="Open main menu"
  menuCloseLabel="Close main menu"
  menuText="Menu"
  navAriaLabel="Primary navigation"
/>
