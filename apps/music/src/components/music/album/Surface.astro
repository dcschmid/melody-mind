<article class="album-page-surface">
  <slot name="hero" />

  <div class="album-page-surface__content">
    <slot />
  </div>
</article>

<style>
  .album-page-surface {
    /* Local aliases onto the global palette so high-contrast and
       forced-colors overrides propagate into this page. */
    --music-album-accent: var(--accent-primary);
    --music-album-accent-strong: var(--accent-strong);
    --music-album-paper: var(--surface-1);
    --music-album-paper-soft: var(--surface-2);
    --music-album-ink: var(--text-primary);
    --music-album-muted: var(--text-tertiary);
    --music-album-line: var(--border-default);

    position: relative;
    inline-size: min(100%, 100vw);
    max-inline-size: 100vw;
    min-block-size: 100svh;
    overflow-x: clip;
    color: var(--music-album-ink);
    isolation: isolate;
  }

  /* Ambient glow fixed to the viewport so it never creates a seam mid-page. */
  .album-page-surface::before {
    position: fixed;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    content: "";
    background: radial-gradient(
      140vw 90vh at 50% -25%,
      color-mix(in srgb, var(--accent-primary) 9%, transparent),
      color-mix(in srgb, var(--accent-strong) 4%, transparent) 45%,
      transparent 80%
    );
  }

  .album-page-surface,
  .album-page-surface *,
  .album-page-surface *::before,
  .album-page-surface *::after {
    box-sizing: border-box;
  }

  .album-page-surface__content {
    position: relative;
    display: grid;
    gap: clamp(0.95rem, 2.6vw, 2rem);
    max-inline-size: 90rem;
    padding: clamp(0.55rem, 1.8vw, 1.4rem) clamp(1rem, 4vw, 4rem) clamp(2rem, 6vw, 5rem);
    margin-inline: auto;
  }

  @media (width <= 760px) {
    .album-page-surface {
      inline-size: 100%;
      max-inline-size: 100vw;
    }

    .album-page-surface__content {
      gap: 0.5rem;
      inline-size: 100%;
      padding: 0.25rem 1rem clamp(2rem, 6vw, 5rem);
      overflow-x: clip;
    }
  }
</style>
