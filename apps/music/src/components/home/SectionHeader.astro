---
import Icon from "@shared-ui/components/visual/Icon.astro";

interface Props {
  headingId: string;
  title: string;
  iconName?: "sparkles" | "disc";
  linkHref?: string;
  linkLabel?: string;
  hideIcon?: boolean;
}

const {
  headingId,
  title,
  iconName = "sparkles",
  linkHref,
  linkLabel,
  hideIcon = false,
} = Astro.props;
---

<div class="music-home-section-header">
  <h2 id={headingId} class="music-home-section-header__title">
    {
      !hideIcon && (
        <Icon
          name={iconName}
          class="music-home-section-header__icon"
          aria-hidden="true"
        />
      )
    }
    {title}
  </h2>
  {
    linkHref && linkLabel && (
      <a class="music-home-section-header__link" href={linkHref}>
        {linkLabel}
        <Icon name="chevron-right" aria-hidden="true" />
      </a>
    )
  }
  <slot />
</div>

<style>
  .music-home-section-header {
    --music-home-section-header-icon-gap: var(--spacing-sm);
    --music-home-section-header-link-gap: var(--spacing-xs);

    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-base);
    row-gap: var(--spacing-xs);
    align-items: baseline;
    justify-content: space-between;
  }

  .music-home-section-header__title {
    display: inline-flex;
    gap: var(--music-home-section-header-icon-gap);
    align-items: center;
    min-inline-size: 0;
    margin: 0;
    font-family: var(--font-sans);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    line-height: 1.15;
    color: var(--music-home-ink);
    letter-spacing: 0;
    text-wrap: balance;
  }

  .music-home-section-header__icon {
    flex: 0 0 auto;
    color: var(--music-home-accent);
  }

  .music-home-section-header__link {
    display: inline-flex;
    flex: 0 0 auto;
    gap: var(--music-home-section-header-link-gap);
    align-items: center;
    justify-content: center;
    min-block-size: 2.5rem;
    padding-inline: var(--spacing-sm);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-bold);
    color: var(--music-home-muted);
    text-decoration: none;
  }

  .music-home-section-header__link:focus-visible {
    outline: var(--focus-ring-width) solid var(--focus-ring-color);
    outline-offset: var(--focus-ring-offset);
  }

  .music-home-section-header__link:hover,
  .music-home-section-header__link:focus-visible {
    color: var(--music-home-accent);
  }

  @media (hover: hover) and (pointer: fine) and (prefers-reduced-motion: no-preference) {
    .music-home-section-header__link {
      transition:
        color var(--motion-duration-fast) var(--motion-ease-standard),
        transform var(--motion-duration-fast) var(--motion-ease-standard);
    }

    .music-home-section-header__link:active {
      transform: scale(0.97);
    }
  }

  @media (width <= 640px) {
    .music-home-section-header {
      align-items: flex-start;
    }
  }
</style>
