<footer class="site-footer">
  <div class="site-footer__inner">
    <div>
      <strong>MelodyMind Reviews</strong>
      <p>Album criticism with a thesis, a byline, and sources readers can inspect.</p>
    </div>
    <nav aria-label="MelodyMind network">
      <a href="https://melody-mind.de/">Music</a>
      <a href="https://stories.melody-mind.de/">Stories</a>
      <a href="/editorial-standards/">Editorial standards</a>
      <a href="/rss.xml">RSS</a>
    </nav>
    <p class="site-footer__legal">© 2026 MelodyMind</p>
  </div>
</footer>

<style>
  .site-footer {
    margin-block-start: var(--space-4xl);
    border-block-start: 1px solid var(--line);
  }

  .site-footer__inner {
    display: grid;
    gap: var(--space-xl);
    inline-size: min(100% - 2rem, var(--wide-width));
    padding-block: var(--space-2xl);
    margin-inline: auto;
  }

  .site-footer strong {
    font-family: var(--font-display);
    font-size: 1.3rem;
  }

  .site-footer p {
    max-inline-size: 48ch;
    margin-block-start: var(--space-xs);
    font-size: 0.9rem;
    color: var(--quiet-ink);
  }

  .site-footer nav {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm) var(--space-xl);
  }

  .site-footer nav a {
    min-block-size: var(--control-size);
    font-weight: 700;
  }

  .site-footer__legal {
    margin: 0;
  }

  @media (width >= 48rem) {
    .site-footer__inner {
      grid-template-columns: minmax(16rem, 1fr) auto;
      align-items: start;
      inline-size: min(100% - 4rem, var(--wide-width));
    }

    .site-footer__legal {
      grid-column: 1 / -1;
    }
  }
</style>
