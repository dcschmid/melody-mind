---
import AppShellLayout from "@shared-ui/components/layout/AppShellLayout.astro";
import type { PageSeoResult } from "@shared-utils/utils/seo/buildPageSeo";
import { knowledgeBrandLogoUrl, knowledgeHeroImageUrl } from "@utils/knowledgeImages";
import { knowledgeAppShellConfig } from "@utils/appShellConfig";
import KnowledgeSearchPalette from "../components/KnowledgeSearchPalette.astro";
import melodyMindLogo from "../assets/melody-mind-logo.webp";

/**
 * Shared layout contract for all knowledge pages.
 * Keep this surface narrow: page-specific SEO should be prepared before reaching the layout.
 */
interface Props {
  imageWidth?: number;
  imageHeight?: number;
  pageSeo: PageSeoResult;
  ogMusic?: {
    creator?: string;
    album?: string;
    musician?: string;
  };
  audioSrc?: string;
  audioType?: string;
  episodeNumber?: number;
  seriesName?: string;
  seriesUrl?: string;
  prevUrl?: string;
  nextUrl?: string;
  disableAlternateLanguages?: boolean;
  authorName?: string;
}

const {
  pageSeo,
  imageWidth,
  imageHeight,
  ogMusic,
  audioSrc,
  audioType,
  episodeNumber,
  seriesName,
  seriesUrl,
  prevUrl,
  nextUrl,
  disableAlternateLanguages,
  authorName,
} = Astro.props;
---

<AppShellLayout
  shellConfig={knowledgeAppShellConfig}
  resolveLogoUrl={knowledgeBrandLogoUrl}
  resolveFallbackImageUrl={knowledgeHeroImageUrl}
  brandLogoSrc={melodyMindLogo}
  brandLogoAlt=""
  {pageSeo}
  {imageWidth}
  {imageHeight}
  {ogMusic}
  {audioSrc}
  {audioType}
  {episodeNumber}
  {seriesName}
  {seriesUrl}
  {prevUrl}
  {nextUrl}
  {disableAlternateLanguages}
  {authorName}
>
  <slot />
  <KnowledgeSearchPalette />
</AppShellLayout>
