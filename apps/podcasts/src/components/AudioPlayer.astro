---
import SharedAudioPlayer from "@shared-ui/components/media/AudioPlayer.astro";

export interface Props {
  title: string;
  audioUrl: string;
  subtitleUrl?: string | undefined;
}

const props = Astro.props as Props;
---

<SharedAudioPlayer {...props} />
