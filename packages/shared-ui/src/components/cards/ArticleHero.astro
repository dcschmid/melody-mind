---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import AIContentBadge from "@shared-ui/components/badges/AIContentBadge.astro";
import ButtonLink from "@shared-ui/components/buttons/ButtonLink.astro";
import Icon from "@shared-ui/components/visual/Icon.astro";
export interface LinkPanelItem {
  href: string;
  label: string;
  icon?: string;
  variant?: "primary" | "secondary";
  ariaLabel?: string;
}

interface Props {
  title: string;
  description: string;
  imageSource?: ImageMetadata;
  imageAlt?: string;
  publishedDate: Date;
  publishedDateLabel: string;
  readingText: string;
  idBase?: string;
  aiGenerated?: boolean;
  aiTools?: string[];
  links?: readonly LinkPanelItem[];
}

const {
  title,
  description,
  imageSource,
  imageAlt = title,
  publishedDate,
  publishedDateLabel,
  readingText,
  idBase = "article-hero",
  aiGenerated = false,
  aiTools = [],
  links = [],
} = Astro.props as Props;

const monthYearLabel = publishedDate.toLocaleDateString("en-US", {
  month: "long",
  year: "numeric",
});
const titleId = `${idBase}-title`;
---

<section class="article-hero" aria-labelledby={titleId}>
  <div class="article-hero__shell">
    <div class="article-hero__layout">
      <div class="article-hero__content">
        <div class="article-hero__topline">
          <ul class="article-hero__meta-list" aria-label="Article details">
            <li class="article-hero__meta-item">
              <Icon name="clock" class="article-hero__meta-icon" aria-hidden="true" />
              <span>{readingText}</span>
            </li>
            <li class="article-hero__meta-item">
              <Icon name="calendar" class="article-hero__meta-icon" aria-hidden="true" />
              <time datetime={publishedDate.toISOString()}>{monthYearLabel}</time>
            </li>
            {
              aiGenerated ? (
                <li class="article-hero__meta-item article-hero__meta-item--badge">
                  <AIContentBadge
                    aiTools={aiTools}
                    size="md"
                    className="article-hero__ai-badge"
                  />
                </li>
              ) : null
            }
          </ul>
        </div>

        <div class="article-hero__intro">
          <h1 id={titleId} class="article-hero__title">{title}</h1>
          <p class="article-hero__description">{description}</p>
        </div>

        <ul class="article-hero__signals" aria-label="Article signals">
          <li class="article-hero__signal">
            <Icon name="eye" class="article-hero__signal-icon" aria-hidden="true" />
            <span>Long-form analysis</span>
          </li>
          <li class="article-hero__signal">
            <Icon name="heart" class="article-hero__signal-icon" aria-hidden="true" />
            <span>Editorially curated</span>
          </li>
          <li class="article-hero__signal">
            <Icon
              name="message-circle"
              class="article-hero__signal-icon"
              aria-hidden="true"
            />
            <span>Updated {publishedDateLabel}</span>
          </li>
        </ul>

        {
          links.length ? (
            <div class="article-hero__links" aria-label="Listen and explore">
              {links.map((link) => (
                <ButtonLink
                  href={link.href}
                  variant={link.variant ?? "secondary"}
                  size="compact"
                  icon={link.icon}
                  aria-label={link.ariaLabel}
                  className="article-hero__link"
                >
                  {link.label}
                </ButtonLink>
              ))}
            </div>
          ) : null
        }
      </div>

      <div class="article-hero__media-column">
        {
          imageSource ? (
            <div class="article-hero__media-frame">
              <Image
                class="article-hero__image"
                src={imageSource}
                alt={imageAlt}
                loading="eager"
                fetchpriority="high"
                width={imageSource.width}
                height={imageSource.height}
                sizes="(max-width: 640px) 100vw, (max-width: 960px) 80vw, 50vw"
                decoding="async"
              />
            </div>
          ) : (
            <div
              class="article-hero__media-placeholder"
              role="img"
              aria-label="No image available"
            >
              <Icon
                name="image"
                class="article-hero__media-placeholder-icon"
                aria-hidden="true"
              />
              <span>No image available</span>
            </div>
          )
        }
      </div>
    </div>
  </div>
</section>

<style>
  .article-hero {
    position: relative;
    contain: layout style;
  }

  .article-hero__shell {
    position: relative;
    padding: 0;
    background: none;
    border: 0;
    border-radius: 0;
    box-shadow: none;
  }

  .article-hero__layout {
    position: relative;
    z-index: 1;
    display: grid;
    gap: var(--stack-gap-lg);
    align-items: stretch;
  }

  .article-hero__content {
    display: grid;
    gap: var(--stack-gap-lg);
    align-content: start;
    min-inline-size: 0;
    color: var(--heading-color);
    overflow-wrap: break-word;
  }

  .article-hero__topline {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm) var(--spacing-base);
    align-items: center;
  }

  .article-hero__meta-list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm) var(--spacing-base);
    align-items: center;
    padding: 0;
    margin: 0;
    list-style: none;
  }

  .article-hero__meta-item {
    display: inline-flex;
    gap: calc(var(--spacing-xs) + 0.08rem);
    align-items: center;
    font-size: var(--font-size-xs);
    color: var(--text-secondary);
  }

  .article-hero__meta-item--badge {
    color: inherit;
  }

  .article-hero__meta-icon {
    inline-size: var(--icon-size-xs);
    block-size: var(--icon-size-xs);
    color: color-mix(in srgb, var(--accent-primary) 35%, var(--text-tertiary));
  }

  .article-hero__intro {
    display: grid;
    gap: var(--stack-gap-md);
  }

  .article-hero__title {
    margin: 0;
    font-size: var(--font-size-2xl);
    font-weight: 800;
    line-height: 1.5;
    color: var(--heading-color);
    letter-spacing: -0.068em;
    text-wrap: balance;
  }

  .article-hero__description {
    margin: 0;
    font-size: var(--font-size-base);
    line-height: 1.64;
    color: var(--text-secondary);
    text-wrap: pretty;
  }

  .article-hero__signals {
    display: flex;
    flex-wrap: wrap;
    gap: calc(var(--spacing-sm) + 0.3rem) calc(var(--spacing-lg) - 0.3rem);
    align-items: center;
    padding: 0;
    padding-inline-start: var(--spacing-sm);
    margin: 0;
    list-style: none;
    border-inline-start: 2px solid
      color-mix(in srgb, var(--accent-primary) 25%, transparent);
  }

  .article-hero__signal {
    display: inline-flex;
    gap: var(--spacing-sm);
    align-items: center;
    font-size: var(--font-size-xs);
    color: var(--text-secondary);
  }

  @media screen and (prefers-reduced-motion: reduce) {
    .article-hero__signal-icon {
      inline-size: var(--icon-size-sm);
      block-size: var(--icon-size-sm);
      color: color-mix(in srgb, var(--accent-primary) 45%, var(--text-tertiary));
      transition: none;
    }
  }

  .article-hero__signal-icon {
    inline-size: var(--icon-size-sm);
    block-size: var(--icon-size-sm);
    color: color-mix(in srgb, var(--accent-primary) 45%, var(--text-tertiary));
    transition: color var(--motion-duration-fast) var(--motion-ease-standard);
  }

  .article-hero__links {
    display: flex;
    flex-wrap: wrap;
    gap: calc(var(--spacing-sm) + var(--spacing-xs));
    align-items: center;
  }

  .article-hero__links :global(.article-hero__link) {
    min-inline-size: 0;
  }

  .article-hero__links :global(.article-hero__link.button-link) {
    min-block-size: 2.25rem;
    padding: var(--control-padding-block-compact)
      calc(var(--control-padding-inline-compact) - 0.05rem);
    font-size: var(--font-size-xs);
    border-radius: var(--control-radius-compact);
  }

  .article-hero__links :global(.article-hero__link .button-link__icon) {
    inline-size: var(--icon-size-xs);
    block-size: var(--icon-size-xs);
  }

  .article-hero__media-column {
    min-inline-size: 0;
  }

  .article-hero__media-frame,
  .article-hero__media-placeholder {
    position: relative;
    overflow: hidden;
    border-radius: clamp(1.75rem, 3vw, 2.2rem);
  }

  .article-hero__media-frame {
    background: var(--surface-panel-raised);
    border: 1px solid var(--surface-border-soft);
    box-shadow: var(--shadow-lg);
  }

  .article-hero__media-frame::after {
    position: absolute;
    inset: 0;
    pointer-events: none;
    content: "";
    background:
      radial-gradient(
        circle at 80% 82%,
        color-mix(in srgb, var(--accent-primary) 18%, transparent),
        transparent 24%
      ),
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface-panel-bg) 8%, transparent),
        transparent 28%
      );
  }

  .article-hero__image,
  .article-hero__image :global(img) {
    display: block;
    inline-size: 100%;
    block-size: auto;
    object-fit: cover;
  }

  .article-hero__media-placeholder {
    display: grid;
    gap: var(--spacing-sm);
    place-items: center;
    padding: var(--spacing-xl);
    color: var(--text-secondary);
    background: var(--surface-panel-bg);
    border: 1px solid var(--surface-border-soft);
  }

  .article-hero__media-placeholder-icon {
    inline-size: var(--icon-size-xl);
    block-size: var(--icon-size-xl);
    color: var(--accent-primary);
  }

  /* -----------------------------------------------------------------------------
     ENTRANCE ANIMATIONS
     Subtle, editorial-grade staggered reveals
     ----------------------------------------------------------------------------- */
  @media (prefers-reduced-motion: no-preference) {
    /* Hero image: subtle scale-in with fade */
    .article-hero__media-frame,
    .article-hero__media-placeholder {
      animation: article-hero-image-in var(--motion-duration-slow)
        var(--motion-ease-standard) var(--motion-delay-none) both;
    }

    /* Content sections: staggered rise-in */
    .article-hero__topline {
      animation: article-hero-rise-in var(--motion-duration-base)
        var(--motion-ease-standard) var(--motion-delay-none) both;
    }

    .article-hero__title {
      animation: article-hero-rise-in var(--motion-duration-base)
        var(--motion-ease-standard)
        calc(var(--motion-delay-base) + var(--motion-delay-fast)) both;
    }

    .article-hero__description {
      animation: article-hero-rise-in var(--motion-duration-base)
        var(--motion-ease-standard)
        calc(var(--motion-delay-slow) + var(--motion-delay-fast)) both;
    }

    .article-hero__signals {
      animation: article-hero-rise-in var(--motion-duration-base)
        var(--motion-ease-standard)
        calc(var(--motion-delay-slow) + var(--motion-delay-base)) both;
    }

    .article-hero__links {
      animation: article-hero-rise-in var(--motion-duration-base)
        var(--motion-ease-standard)
        calc(var(--motion-delay-slow) + var(--motion-delay-slow)) both;
    }
  }

  @keyframes article-hero-rise-in {
    from {
      opacity: var(--motion-opacity-soft);
      transform: translateY(var(--motion-distance-md));
    }

    to {
      opacity: var(--motion-opacity-full);
      transform: translateY(0);
    }
  }

  @keyframes article-hero-image-in {
    from {
      opacity: var(--motion-opacity-soft);
      transform: scale(1.02);
    }

    to {
      opacity: var(--motion-opacity-full);
      transform: scale(1);
    }
  }

  /* -----------------------------------------------------------------------------
     MICRO-INTERACTIONS
     Subtle hover feedback for links and signals
     ----------------------------------------------------------------------------- */
  @media (prefers-reduced-motion: no-preference) {
    /* Links: gentle lift on hover */
    .article-hero__links :global(.article-hero__link) {
      transition:
        transform var(--motion-duration-fast) var(--motion-ease-standard),
        box-shadow var(--motion-duration-fast) var(--motion-ease-standard);
    }

    .article-hero__links :global(.article-hero__link:hover),
    .article-hero__links :global(.article-hero__link:focus) {
      transform: translateY(var(--motion-distance-xs));
    }

    .article-hero__links :global(.article-hero__link.button-link:hover),
    .article-hero__links :global(.article-hero__link.button-link:focus) {
      box-shadow: var(--shadow-md);
    }

    /* Signals: icon color shift on hover/focus */
    .article-hero__signal {
      transition: color var(--motion-duration-fast) var(--motion-ease-standard);
    }

    .article-hero__signal:hover .article-hero__signal-icon,
    .article-hero__signal:focus-visible .article-hero__signal-icon {
      color: var(--accent-primary);
    }

    .article-hero__signal:focus-visible {
      outline: 2px solid color-mix(in srgb, var(--accent-primary) 50%, transparent);
      outline-offset: 2px;
      border-radius: var(--control-radius-compact);
    }

    /* Media placeholder: subtle glow on icon */
    .article-hero__media-placeholder-icon {
      transition:
        opacity var(--motion-duration-slow) var(--motion-ease-standard),
        transform var(--motion-duration-slow) var(--motion-ease-standard);
    }

    .article-hero__media-placeholder:hover .article-hero__media-placeholder-icon,
    .article-hero__media-placeholder:focus .article-hero__media-placeholder-icon {
      opacity: var(--motion-opacity-full);
      transform: scale(1.05);
    }
  }

  @media (width >= 960px) {
    .article-hero__layout {
      grid-template-columns: minmax(24rem, 1fr) minmax(34rem, 1.02fr);
      gap: clamp(var(--spacing-xl), 2.8vw, calc(var(--spacing-3xl) - 0.75rem));
      min-block-size: 18.5rem;
    }

    .article-hero__title {
      font-size: var(--font-size-2xl);
    }

    .article-hero__content {
      gap: var(--stack-gap-lg);
      padding-block-start: calc(var(--spacing-xs) - 0.15rem);
    }

    .article-hero__topline {
      gap: calc(var(--spacing-sm) + 0.05rem) calc(var(--spacing-base) - 0.1rem);
    }

    .article-hero__signals {
      gap: calc(var(--spacing-sm) + 0.35rem) calc(var(--spacing-lg) - 0.05rem);
      margin-block-start: 0;
    }

    .article-hero__media-column {
      display: flex;
      align-items: center;
      padding-block-start: var(--spacing-xs);
    }

    .article-hero__media-frame,
    .article-hero__media-placeholder {
      inline-size: 100%;
      min-block-size: 0;
      max-block-size: none;
      aspect-ratio: 3 / 2;
    }
  }

  @media (width >= 640px) and (width < 960px) {
    .article-hero__layout {
      gap: clamp(var(--spacing-lg), 2vw, var(--spacing-xl));
    }

    .article-hero__content {
      gap: var(--stack-gap-md);
    }

    .article-hero__topline {
      gap: calc(var(--spacing-sm) + 0.05rem) calc(var(--spacing-base) - 0.1rem);
    }

    .article-hero__signals {
      gap: calc(var(--spacing-sm) + 0.25rem) calc(var(--spacing-lg) - 0.15rem);
    }
  }

  @media (width < 640px) {
    .article-hero__content {
      order: 2;
    }

    .article-hero__media-column {
      order: 1;
      padding-block-start: 0;
      margin-block-end: var(--spacing-sm);
    }

    .article-hero__shell {
      padding: 0;
      border-radius: 0;
    }

    .article-hero__title {
      font-size: var(--font-size-2xl);
      line-height: 1.15;
    }

    .article-hero__description {
      font-size: var(--font-size-md);
    }

    .article-hero__signals {
      gap: var(--spacing-sm) var(--spacing-base);
    }

    .article-hero__signal {
      font-size: var(--font-size-xs);
    }

    .article-hero__links {
      gap: var(--spacing-sm);
    }

    .article-hero__links :global(.article-hero__link.button-link) {
      min-block-size: 2.5rem;
      padding: calc(var(--control-padding-block-compact) - 0.05rem)
        calc(var(--control-padding-inline-compact) - 0.15rem);
      font-size: var(--font-size-xs);
    }

    .article-hero__media-frame,
    .article-hero__media-placeholder {
      min-block-size: 0;
      aspect-ratio: 3 / 2;
    }
  }
</style>
