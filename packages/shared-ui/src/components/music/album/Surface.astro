<article class="album-page-surface">
  <slot name="hero" />

  <div class="album-page-surface__content">
    <slot />
  </div>
</article>

<style>
  .album-page-surface {
    --music-album-accent: oklch(40% 0.13 16deg);
    --music-album-accent-strong: oklch(38% 0.125 16deg);
    --music-album-paper: oklch(98% 0.014 75deg);
    --music-album-paper-soft: oklch(94% 0.032 70deg);
    --music-album-ink: oklch(19% 0.025 52deg);
    --music-album-muted: oklch(39% 0.033 55deg);
    --music-album-line: oklch(83% 0.043 67deg);

    position: relative;
    inline-size: min(100%, 100vw);
    max-inline-size: 100vw;
    min-block-size: 100svh;
    overflow-x: clip;
    color: var(--music-album-ink);
    isolation: isolate;
  }

  .album-page-surface,
  .album-page-surface *,
  .album-page-surface *::before,
  .album-page-surface *::after {
    box-sizing: border-box;
  }

  :global([data-theme="dark"]) .album-page-surface {
    --music-album-accent: oklch(78% 0.16 286deg);
    --music-album-accent-strong: oklch(80% 0.13 252deg);
    --music-album-paper: oklch(18% 0.037 255deg);
    --music-album-paper-soft: oklch(22% 0.041 252deg);
    --music-album-ink: oklch(96% 0.012 72deg);
    --music-album-muted: oklch(76% 0.036 252deg);
    --music-album-line: oklch(36% 0.055 252deg);
  }

  .album-page-surface__content {
    position: relative;
    display: grid;
    gap: clamp(0.95rem, 2.6vw, 2rem);
    max-inline-size: 90rem;
    padding: clamp(0.55rem, 1.8vw, 1.4rem) clamp(1rem, 4vw, 4rem) clamp(2rem, 6vw, 5rem);
    margin-inline: auto;
  }

  @media (width <= 760px) {
    .album-page-surface {
      inline-size: 100%;
      max-inline-size: 100vw;
    }

    .album-page-surface__content {
      gap: 0.5rem;
      inline-size: 100%;
      padding: 0.25rem 1rem clamp(2rem, 6vw, 5rem);
      overflow-x: clip;
    }
  }
</style>
