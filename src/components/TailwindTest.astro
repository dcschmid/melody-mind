---
// Test component to verify Tailwind CSS 4 is working correctly
---

<div class="bg-gradient-to-r from-purple-500 to-pink-500 p-8 rounded-lg shadow-lg max-w-md mx-auto mt-10">
  <h1 class="text-white text-3xl font-bold mb-4">
    Tailwind CSS 4 Test
  </h1>

  <p class="text-white/80 mb-6">
    This component tests Tailwind CSS 4 features including:
  </p>

  <ul class="text-white space-y-2 mb-6">
    <li class="flex items-center">
      <span class="w-2 h-2 bg-green-400 rounded-full mr-3"></span>
      Gradient backgrounds
    </li>
    <li class="flex items-center">
      <span class="w-2 h-2 bg-green-400 rounded-full mr-3"></span>
      Modern spacing utilities
    </li>
    <li class="flex items-center">
      <span class="w-2 h-2 bg-green-400 rounded-full mr-3"></span>
      Opacity modifiers (text-white/80)
    </li>
    <li class="flex items-center">
      <span class="w-2 h-2 bg-green-400 rounded-full mr-3"></span>
      Responsive design
    </li>
  </ul>

  <!-- Test CSS variables integration -->
  <div class="bg-[var(--bg-secondary)] text-[var(--text-primary)] p-4 rounded-md mb-4">
    <p class="font-semibold">CSS Variables Integration:</p>
    <p class="text-sm opacity-90">
      This uses your existing CSS variables with Tailwind's arbitrary value syntax.
    </p>
  </div>

  <!-- Test hover and focus states -->
  <button class="bg-white text-purple-600 px-6 py-2 rounded-full font-semibold
                 hover:bg-purple-50 hover:scale-105
                 focus:outline-none focus:ring-4 focus:ring-purple-300
                 transition-all duration-200 ease-in-out
                 active:scale-95">
    Interactive Button
  </button>
</div>

<!-- Test responsive design -->
<div class="mt-10 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto px-4">
  <div class="bg-blue-100 p-6 rounded-lg">
    <h3 class="text-blue-900 font-bold mb-2">Mobile First</h3>
    <p class="text-blue-700">This card shows on all sizes</p>
  </div>

  <div class="bg-green-100 p-6 rounded-lg">
    <h3 class="text-green-900 font-bold mb-2">Tablet+</h3>
    <p class="text-green-700">Two columns on md+ screens</p>
  </div>

  <div class="bg-amber-100 p-6 rounded-lg">
    <h3 class="text-amber-900 font-bold mb-2">Desktop</h3>
    <p class="text-amber-700">Three columns on large screens</p>
  </div>
</div>

<style>
  /* Test that custom CSS still works alongside Tailwind */
  .custom-animation {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
</style>
