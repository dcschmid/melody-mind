---
const { headline, subline, whiteText, isSmallHeadline = false, isWhiteSubline = false } = Astro.props;
---

<div class="introText">
  <div class:list={["headline", isSmallHeadline && "isSmallHeadline"]}>{headline}</div>
  <div class:list={["subline", isWhiteSubline && "whiteText"]}>
    {subline}
    <span class="whiteText">{whiteText}</span>
  </div>
</div>

<style>
  .introText {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-2);
    margin-bottom: var(--spacing-s);
  }

  .headline {
    font-size: 24px;
    font-style: normal;
    font-weight: 900;
    line-height: 120%;
    letter-spacing: 0.36px;
  }

  .isSmallHeadline {
    font-size: 14px;
    font-style: normal;
    font-weight: 700;
    line-height: 120%;
  }

  .subline {
    color: var(--accent-color-4);
    font-size: 1.125rem;
    font-style: normal;
    font-weight: 400;
    line-height: 150%;
    text-align: center;
    width: 386px;
    max-width: 100%;
  }

  .whiteText {
    color: var(--text-color);
  }
</style>
