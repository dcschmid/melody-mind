---
import { getRelativeLocaleUrl } from "astro:i18n";
import { determineUserLanguage } from "../utils/languageUtils.ts";

const preferredLocale = determineUserLanguage();
Astro.redirect(getRelativeLocaleUrl(preferredLocale, "/"));
---
